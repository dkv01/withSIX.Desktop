// <copyright company="SIX Networks GmbH" file="SteamDownloaderTest.cs">
//     Copyright (c) SIX Networks GmbH. All rights reserved. Do not remove this notice.
// </copyright>

namespace withSIX.Mini.Tests
{
    /*
    [TestFixture]
    public class SteamDownloaderTest : BaseTest<SteamDownloader>
    {
        class JsonInfo
        {
            public string UserName { get; set; }
            public string Password { get; set; }
        }

        [SetUp]
        public void SetUp() {
            Common.AppCommon.SetAppName(Consts.InternalTitle);
            CommonBase.AssemblyLoader = new AssemblyLoader(typeof(SteamDownloaderTest).Assembly);
            Common.Paths = new PathConfiguration();
            Common.Paths.SetPaths();
        }

        [Test]
        public void Create() {
            SUT = new SteamDownloader();
        }

        [Test]
        public async Task DownloadContent() {
            var contentId = uint.Parse("450814997");
            SUT = new SteamDownloader();
            var secret =
                Tools.Serialization.Json.LoadJsonFromFile<JsonInfo>("C:\\temp\\secret.json".ToAbsoluteFilePath());
            //await SUT.Download(contentId, "C:\\temp\\somewhere".ToAbsoluteDirectoryPath(), new LoginDetails(secret.UserName, secret.Password), (l, d) => Console.WriteLine($"{l}b/s {d}%")) .ConfigureAwait(false);
        }
    }
    */
}