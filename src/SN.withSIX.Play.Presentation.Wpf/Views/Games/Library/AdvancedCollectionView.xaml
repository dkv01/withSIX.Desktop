<UserControl x:Class="SN.withSIX.Play.Presentation.Wpf.Views.Games.Library.AdvancedCollectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel>
            <StackPanel>
                <ListBox x:Name="Servers" />
                <Button Content="Add Server" x:Name="AddServer" />
            </StackPanel>

            <StackPanel>
                <ListBox x:Name="TsServers" />
                <Button Content="Add TS Server" x:Name="AddTsServer" />
            </StackPanel>

            <StackPanel>
                <ListBox x:Name="CustomRepos" />
                <Button Content="Add Custom Repo" x:Name="AddCustomRepo" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>